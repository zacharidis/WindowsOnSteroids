<Window x:Class="WindowsOnSteroids.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Windows On Steroids" Height="720" Width="880"
        MinHeight="520" MinWidth="700"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BgBrush}">

    <Window.Resources>

        <!-- ── Toggle Switch ─────────────────────────────────────── -->
        <Style x:Key="ToggleSwitch" TargetType="CheckBox">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid VerticalAlignment="Center" Width="44" Height="24">
                            <Border x:Name="Track" CornerRadius="12"
                                    Background="{DynamicResource ToggleOffBrush}"/>
                            <Ellipse x:Name="Thumb" Width="18" Height="18"
                                     Fill="White" HorizontalAlignment="Left"
                                     Margin="3,0,0,0">
                                <Ellipse.Effect>
                                    <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.3"/>
                                </Ellipse.Effect>
                            </Ellipse>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="#0078D4"/>
                                <Setter TargetName="Thumb" Property="Margin" Value="23,0,0,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── Rounded Button ────────────────────────────────────── -->
        <Style x:Key="RoundBtn" TargetType="Button">
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header  -->
            <RowDefinition Height="*"/>
            <!-- Content -->
            <RowDefinition Height="Auto"/>
            <!-- Status  -->
        </Grid.RowDefinitions>

        <!-- ── Header ──────────────────────────────────────────── -->
        <Border Grid.Row="0" Background="{DynamicResource HeaderBrush}" Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="🛠  Windows On Steroids" FontSize="22" FontWeight="Bold"
                               Foreground="{DynamicResource FgBrush}"/>
                    <TextBlock FontSize="12" Margin="0,4,0,0"
                               Foreground="{DynamicResource SubtleBrush}"
                               Text="Registry-based system tweaks — running as Administrator"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="✓  Apply All"
                            Style="{StaticResource RoundBtn}"
                            Background="#0078D4" Foreground="White"
                            Click="ApplyAll_Click" Margin="0,0,8,0"/>
                    <Button Content="↺  Restore Defaults"
                            Style="{StaticResource RoundBtn}"
                            Background="{DynamicResource BtnBrush}"
                            Foreground="{DynamicResource FgBrush}"
                            Click="RestoreDefaults_Click" Margin="0,0,8,0"/>
                    <Button Content="✕  Reset All"
                            Style="{StaticResource RoundBtn}"
                            Background="#C42B1C" Foreground="White"
                            Click="ResetAll_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ── Tweaks List ──────────────────────────────────────── -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20,4,20,12">
            <StackPanel x:Name="TweaksPanel" Margin="0,8,0,0"/>
        </ScrollViewer>

        <!-- ── Status Bar ──────────────────────────────────────── -->
        <Border Grid.Row="2" Background="{DynamicResource HeaderBrush}" Padding="20,8">
            <Grid>
                <TextBlock x:Name="StatusText" Text="Ready — hover a toggle to apply a tweak."
                           FontSize="12" VerticalAlignment="Center"
                           Foreground="{DynamicResource SubtleBrush}"/>
                <TextBlock x:Name="LastApplied" FontSize="12" HorizontalAlignment="Right"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource SubtleBrush}"/>
            </Grid>
        </Border>
    </Grid>
</Window>